	1. Firstly there should be public class same as the file name.
	2. class should consist of main function which is entry point of the programme
	3. main is parameterized function with string[] args which are command line args which we can give while executing the main
	4. also main should always be public static and should not return anything that is it will return void as when we run the code it runs the main without creating object.

### Data Types : 
		1. byte:
				8 bits of memory
				-128 to 127 is the range